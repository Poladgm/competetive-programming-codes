#include <cstdlib>
#include <iostream>

using namespace std;

int main(int argc, char *argv[])
{
   long long a,b,c;
   while(cin>>a>>b>>c)
   {
      if(a==0 && b==0 && c==0)
   {
               return 0;
   }
      else if(a*a==b*b+c*c || b*b==a*a+c*c || c*c==a*a+b*b)
   cout<<"right"<<endl;

   else
   cout<<"wrong"<<endl;

}
     return EXIT_SUCCESS;
}